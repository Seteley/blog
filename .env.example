# Copiar a .env — no commitear con credenciales reales

# Local (docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blogdb
DB_USER=postgres
DB_PASSWORD=postgres

# Render lo inyecta automáticamente — no hace falta setearlo a mano
# DATABASE_URL=postgresql://user:pass@host:port/dbname
